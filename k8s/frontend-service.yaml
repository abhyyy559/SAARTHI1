apiVersion: v1
kind: Service
metadata:
  name: saarthi-frontend
  namespace: saarthi
  labels:
    app.kubernetes.io/name: saarthi-frontend
    app.kubernetes.io/part-of: saarthi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: saarthi-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
