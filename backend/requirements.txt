fastapi==0.115.0
uvicorn==0.30.6
# Range, not a hard pin: pydantic-core is compiled (Rust) and only ships
# prebuilt wheels for Python versions that existed at its release. A hard
# 2.9.2 pin has no wheel for Render's newer native runtimes (3.13/3.14) and
# falls back to a source build, which fails without Rust. Any pydantic 2.x
# keeps our API surface (BaseModel, model_dump) intact.
pydantic>=2.9,<3
httpx==0.27.2
python-multipart==0.0.12
cryptography==44.0.0
python-dotenv==1.0.1
# Web Push (background notifications when the app is CLOSED).
# Pulls in py-vapid + http-ece for the VAPID JWT and aes128gcm payload
# encryption; hand-rolling those is how you ship a push nobody receives.
pywebpush==2.0.3
# PostgreSQL persistence (DATABASE_URL). Optional at runtime — the stores fall
# back to JSON files when this import fails — but listed here so a deployment
# with a database URL never discovers the missing driver on stage.
asyncpg>=0.29,<1
